# Preprocess ablation

Compares preprocess variants (`none`, `invert`, `clahe`, …) on the **same trials** in one table.

```bash
./setup.sh && source .venv/bin/activate
jupyter notebook easyocr_preprocess_ab.ipynb
```

Images: `../images/`. Kernel: **Sportify EasyOCR preprocess ablation**.
