# Throughput, errors, and visualization

Same as **throughput-and-errors**, plus in-notebook images with OCR bounding boxes on failures.

```bash
./setup.sh && source .venv/bin/activate
jupyter notebook easyocr_bench.ipynb
```

Images: `../images/`. Kernel: **Sportify EasyOCR throughput viz**.
