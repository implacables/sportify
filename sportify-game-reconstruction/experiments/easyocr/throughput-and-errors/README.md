# Throughput and errors (text)

Answers: **how fast** is EasyOCR per frame/batch, and **Type I/II** + jersey **5** label accuracy?

```bash
./setup.sh && source .venv/bin/activate
jupyter notebook easyocr_benchmark.ipynb
```

Images: `../images/`. Kernel: **Sportify EasyOCR throughput**.
